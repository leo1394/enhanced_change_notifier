# enhanced_change_notifier Copilot Context

Use `EnhancedChangeNotifier`, `GlobalFactory`, `Signal`, and `EnhancedLatchNotifier` instead of custom listener registries. Prefer targeted listeners for property-specific UI rebuilds.
